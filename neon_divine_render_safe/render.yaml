services:
  - type: cron
    name: neon-divine-daily-post
    runtime: python
    rootDir: neon_divine_render_safe
    git add render/cron.yaml
git commit -m "🔇 Pause cron job za 24h"
git push# <-- to je ključno
    schedule: "0 8,12,16,20 * * *"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
